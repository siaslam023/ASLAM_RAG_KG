# View App Logs / Debugging
docker logs -f kg-rag-container

#Restart Application Container
docker restart kg-rag-container

#Update App Code After Git Push
cd ~/ASLAM_RAG_KG
git pull origin main
docker build -t kg-rag-app .
docker stop kg-rag-container && docker rm kg-rag-container
docker run -d -p 8501:8501 --env-file .env --restart always --name kg-rag-container kg-rag-app


#Verify SSL Auto-Renewal Status
certbot renew --dry-run

